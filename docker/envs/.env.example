# ===========================================
# NotifyHub - Docker Environment Configuration
# ===========================================

# 應用程式設定
APP_NAME=notifyhub
COMPOSE_PROJECT_NAME=notifyhub
APP_ENV=development

# ===========================================
# 對外 Port 設定
# ===========================================

# 前端 (單一入口，內部藍綠切換)
FRONTEND_PORT=9103

# 後端 API
BACKEND_PORT=9203

# phpMyAdmin
PHPMYADMIN_PORT=9303

# WebSocket
WEBSOCKET_PORT=8080
# phpMyAdmin 完整網址（用於反向代理，務必包含斜線結尾）
# Development (直接存取): http://localhost:${PHPMYADMIN_PORT}/
# Production (透過 /pma 路徑): https://your-domain.com/pma/
PMA_ABSOLUTE_URI=http://localhost:9303/

# ===========================================
# MariaDB 設定
# ===========================================

MARIADB_ROOT_PASSWORD=notifyhub_root_2024
MARIADB_DATABASE=notifyhub
MARIADB_USER=notifyhub
MARIADB_PASSWORD=notifyhub_db_2024

# ===========================================
# CodeIgniter 4 設定
# ===========================================

CI_ENVIRONMENT=development
APP_BASEURL=http://localhost:8080

# JWT 設定
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================================
# 藍綠部署控制
# ===========================================
# 當前活躍的前端版本: blue 或 green
ACTIVE_FRONTEND=blue

# ===========================================
# 前端 API 設定
# ===========================================
# 前端呼叫後端 API 的完整網址（Build 時注入）
# Development: http://localhost:${BACKEND_PORT}/api
# Production: https://your-domain.com/api
VITE_API_URL=http://localhost:9203/api
