# ===========================================
# NotifyHub - 開發環境自動部署
# 透過 SSH 連線到伺服器執行 git pull + shell script
# ===========================================

name: Deploy to Development

on:
  push:
    branches:
      - develop
  workflow_dispatch:  # 支援手動觸發

env:
  DEPLOY_PATH: ${{ secrets.DEV_DEPLOY_PATH }}

jobs:
  deploy:
    name: Deploy to Dev Server
    runs-on: ubuntu-latest
    
    steps:
      - name: 顯示部署資訊
        run: |
          echo "🚀 開始部署到開發環境"
          echo "分支: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "觸發者: ${{ github.actor }}"

      - name: SSH 連線並部署
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USER }}
          key: ${{ secrets.DEV_SSH_KEY }}
          port: ${{ secrets.DEV_SSH_PORT || 22 }}
          script: |
            echo "=========================================="
            echo "  NotifyHub 開發環境部署"
            echo "=========================================="
            
            # 切換到專案目錄
            cd ${{ secrets.DEV_DEPLOY_PATH }}
            
            # 顯示當前狀態
            echo ""
            echo "[1/6] 當前 Git 狀態:"
            echo "  分支: $(git rev-parse --abbrev-ref HEAD)"
            echo "  Commit: $(git rev-parse --short HEAD)"
            
            # 拉取最新代碼
            echo ""
            echo "[2/6] 拉取最新代碼..."
            git fetch origin
            git pull origin develop
            
            echo ""
            echo "[3/6] 更新後 Git 狀態:"
            echo "  Commit: $(git rev-parse --short HEAD)"
            
            # [4/6] 設定環境變數
            echo ""
            echo "[4/6] 設定環境變數..."
            
            if [ ! -f docker/envs/.env.development ]; then
              cp docker/envs/.env.example docker/envs/.env.development
              echo "  已從範例建立 .env.development"
              
              NEW_JWT_SECRET=$(openssl rand -base64 32 | tr -dc 'a-zA-Z0-9' | head -c 48)
              sed -i "s|^JWT_SECRET=.*|JWT_SECRET=${NEW_JWT_SECRET}|" docker/envs/.env.development
            fi
            
            # 使用部署腳本同步並啟動
            echo ""
            echo "[5/6] 執行部署程序..."
            chmod +x ./scripts/*.sh
            
            # 開發環境通常直接部署到 blue 即可，或按照需求
            ./scripts/deploy.sh development
            
            # [6/6] 服務狀態
            echo ""
            echo "[6/6] 服務狀態:"
            docker-compose -f docker/docker-compose.yml ps
            
            echo ""
            echo "=========================================="
            echo "  ✅ 開發環境部署完成！"
            echo "=========================================="

      - name: 部署成功通知
        if: success()
        run: |
          echo "✅ 開發環境部署成功"
          curl -X POST https://notify.try-8verything.com/api/notifications/windows \
            -H "Content-Type: application/json" \
            -H "X-API-Key: ${{ secrets.WINDOWS_NOTIFY_API_KEY }}" \
            -d '{
              "title": "✅ Dev Deploy Success",
              "message": "開發環境部署成功！\n${{ github.event.head_commit.message }}",
              "repo": "${{ github.repository }}",
              "branch": "${{ github.ref_name }}",
              "commit_sha": "${{ github.sha }}",
              "action_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }'

      - name: 部署失敗通知
        if: failure()
        run: |
          echo "❌ 開發環境部署失敗"
          curl -X POST https://notify.try-8verything.com/api/notifications/windows \
            -H "Content-Type: application/json" \
            -H "X-API-Key: ${{ secrets.WINDOWS_NOTIFY_API_KEY }}" \
            -d '{
              "title": "❌ Dev Deploy Failed",
              "message": "開發環境部署失敗！\n${{ github.event.head_commit.message }}",
              "repo": "${{ github.repository }}",
              "branch": "${{ github.ref_name }}",
              "commit_sha": "${{ github.sha }}",
              "action_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }'
